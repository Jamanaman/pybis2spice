[project]
name="pybis2spice"
dependencies = [
    "ecdtools",
    "numpy",
    "matplotlib"
    ]
requires-python = ">=3.7"
dynamic = ["version"]
readme = "README.md"
authors = [
    { name = "Kishan Amratia" },
    { name = "Thomas Damjanovic"}
]
maintainers = [
    { name = "Thomas Damjanovic"}
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.optional-dependencies]
gui=[
    "tkinter-tooltip"
    ]
build=["pyinstaller"]
test=["unittest"]

[tool.hatch.version]
path="version.py"

[tool.mypy]
python_version = "3.10"